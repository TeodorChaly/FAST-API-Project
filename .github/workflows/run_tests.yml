name: Run automation tests

on:
  workflow_dispatch:

jobs:
  Test-Run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run Test
        run: python automation_tests/test.py
